# Runtime dependencies for the deployed dashboard.
#
# Ranges, not exact pins. Pinning numpy==2.0.2 conflicted with nba_api's numpy>=2.1.0 and made
# the set unresolvable, and the deploy host runs Python 3.14, which has no prebuilt wheel for
# that numpy — pip fell back to compiling from source and the build failed.
#
# nba_api and lightgbm are deliberately absent here: neither is needed to SERVE predictions.
# See requirements-dev.txt to run the data pipeline or refit the models.

streamlit>=1.40
altair>=5.4
pandas>=2.2
numpy>=1.26
pyarrow>=16.0            # pandas parquet engine; not imported directly
scikit-learn>=1.5,<1.8   # upper bound: the shipped model pickle was fitted with 1.6.1
