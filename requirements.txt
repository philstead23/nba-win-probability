# Runtime dependencies for the dashboard.
#
# Ranges, not exact pins. Exact pins broke the deploy twice over: numpy==2.0.2 conflicted with
# nba_api's numpy>=2.1.0 and made the set unresolvable, and no prebuilt numpy 2.0.2 exists for
# the Python 3.14 the host runs, so pip fell back to compiling it from source.
#
# nba_api and lightgbm are deliberately absent. Neither is needed to serve predictions —
# nba_api supplied one static lookup table (now inlined in src/features.py) and lightgbm is
# only used to fit the gradient-boosted comparison model, which the dashboard never loads.
# Re-add both to run the full training pipeline.

streamlit>=1.40
altair>=5.4
pandas>=2.2
numpy>=1.26
pyarrow>=16.0          # pandas parquet engine; not imported directly
scikit-learn>=1.5,<1.8  # upper bound: the shipped model pickle was fitted with 1.6.1
